<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->

        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>

        <item name="android:windowActionModeOverlay">true</item>
        <item name="windowActionModeOverlay">true</item>

        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>

        <item name="actionModeBackground">@color/medium_gray</item>
        <item name="android:actionModeBackground">@color/medium_gray</item>

        <item name="android:windowBackground">@color/primary_dark</item>
        <item name="android:colorBackground">@color/primary_dark</item>
    </style>

    <style name="AppTheme" parent="AppBaseTheme">
    </style>

    <style name="ActionBarTheme" parent="AppBaseTheme">
        <item name="android:windowActionBar">true</item>
        <item name="windowActionBar">true</item>
    </style>

    <style name="DateArrow">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:scaleType">center</item>
        <item name="android:tint">@android:color/white</item>
    </style>

    <style name="PasscodeStyle">
        <item name="android:layout_width">65dp</item>
        <item name="android:layout_height">65dp</item>
        <item name="android:background">@drawable/passcode_circle</item>
        <item name="android:layout_margin">@dimen/half_spacer</item>
        <item name="android:textSize">25sp</item>
        <item name="android:hapticFeedbackEnabled">true</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
